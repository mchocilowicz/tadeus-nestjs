export enum Status {
    ACTIVE = 'ACTIVE',
    SUSPENDED = 'SUSPENDED',
    BLOCKED = 'BLOCKED',
    NOT_ACTIVE = 'NOT_ACTIVE',
    DELETED = 'DELETED'
}

export enum Step {
    CODE = 'CODE',
    ACTIVE = 'ACTIVE',
    SIGN_IN = 'SIGN_IN'
}

export enum TransactionStatus {
    REJECTED = 'REJECTED',
    ACCEPTED = 'ACCEPTED',
    CORRECTED = 'CORRECTED',
    WAITING = 'WAITING'
}
